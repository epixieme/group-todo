<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>

<body>
    <h1>Todos</h1>
    <ul>
        <% todos.forEach( el=> { %> <li class='todoItem' data-id='<%=el._id%>'>
            <input type="image" class="<%= el.completed === true ? "filter-green" : "not"%>"
                src="<%= el.completed === true ? "/images/check-square-fill.svg" : "/images/square.svg"%>" alt="">
            <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.todo %></span>
            <input type="image" class='del' src="/images/trash.svg">
            <!-- temporary buttons that can be changed to svgs -->
            <button class ='edit'>Edit</button>
            <button class ='update'>Update</button>
        </li>
        <% }) %>
    </ul>

    <h2><%= user.userName %> has <%= left %> things left to do.</h2>

    <form action="/todos/createTodo" method='POST'>
        <input type="text" placeholder="Enter Todo Item" name='todoItem'>
        <input type="submit">
    </form>

    <a href="/logout">Logout</a>

    <script src="js/main.js"></script>
</body>

</html>